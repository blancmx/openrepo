# 个人博客系统 — 产品需求文档 (PRD)

## 1. 项目背景与目标

### 1.1 背景
作为全栈开发入门练习项目，通过完整实现一个个人博客系统，掌握前后端联调、数据库设计、API 开发、部署上线等全栈开发核心技能。

### 1.2 项目目标
- 独立完成一个可公开访问、功能完整的个人博客
- 掌握 CRUD（增删改查）的完整开发流程
- 具备可以放入作品集（Portfolio）、参与求职或开源社区展示的成品
- 为后续参与开源项目积累实战经验

### 1.3 非目标（本阶段不做）
- 多用户系统（评论区、点赞等社交功能）
- 复杂的富文本编辑器（先用 Markdown 代替）
- SEO 优化、多语言支持

---

## 2. 用户与场景

| 角色 | 描述 | 核心诉求 |
|---|---|---|
| 博主（我自己） | 唯一的内容创作者 | 方便地撰写、编辑、发布、删除文章 |
| 访客 | 任何浏览博客的人 | 顺畅地浏览文章列表、阅读文章详情 |

---

## 3. 功能需求

### 3.1 MVP（最小可行产品）功能列表

| 优先级 | 功能模块 | 功能点 | 说明 |
|---|---|---|---|
| P0 | 文章列表 | 展示所有文章的标题、摘要、发布时间 | 首页核心功能 |
| P0 | 文章详情 | 点击文章可查看完整内容 | 支持 Markdown 渲染 |
| P0 | 新建文章 | 填写标题+正文，提交后保存 | 无需登录（个人使用阶段） |
| P0 | 删除文章 | 可删除已发布的文章 | 需二次确认，防止误删 |
| P1 | 编辑文章 | 修改已有文章的标题/内容 | |
| P1 | 加载与错误状态 | 请求中显示 loading，请求失败给出提示 | 提升体验健壮性 |
| P2 | 登录鉴权 | 仅博主本人可发布/编辑/删除 | 用 JWT 实现 |
| P2 | 标签/分类 | 文章可打标签，支持按标签筛选 | |
| P2 | 搜索文章 | 按标题/内容关键词搜索 | |
| P3 | 部署上线 | 前后端均可公网访问 | 作品集展示必备 |

### 3.2 功能详细说明

#### 3.2.1 文章列表页
- 展示所有文章，按发布时间倒序排列
- 每条展示：标题、发布日期、内容摘要（前 100 字）
- 点击标题跳转到文章详情页

#### 3.2.2 文章详情页
- 展示文章完整标题、正文（Markdown 渲染为 HTML）、发布时间
- 提供"编辑"和"删除"入口（P1/P0 阶段暂不做权限区分，后续接入登录后仅博主可见）

#### 3.2.3 写文章 / 编辑文章页
- 表单字段：标题（必填，1-100字）、正文（必填，支持 Markdown 语法）
- 提交后跳转回文章详情页或列表页
- 编辑页需预填已有内容

#### 3.2.4 删除文章
- 删除前弹出二次确认弹窗
- 删除后从列表中移除，跳转回列表页

---

## 4. 非功能需求

| 类别 | 要求 |
|---|---|
| 性能 | 文章列表首屏加载 < 2 秒（本地环境） |
| 兼容性 | 支持主流浏览器最新版（Chrome/Safari/Edge） |
| 响应式 | 适配桌面端和移动端浏览 |
| 安全性 | 后端接口做基础输入校验，防止空标题/超长内容写入 |
| 可维护性 | 前后端代码分离，接口文档清晰，便于后续扩展 |

---

## 5. 技术方案

### 5.1 技术栈

| 层级 | 技术选型 | 备注 |
|---|---|---|
| 前端 | React + react-router | 后续可加 Tailwind CSS 美化 |
| 后端 | Node.js + Express | RESTful API 设计 |
| 数据库 | SQLite（起步）→ 可迁移 MongoDB/PostgreSQL | 新手友好，无需额外安装数据库服务 |
| Markdown 渲染 | react-markdown | 前端渲染文章内容 |
| 身份验证（P2阶段） | JWT | 仅博主本人可操作写/改/删 |
| 部署 | 前端 Vercel / 后端 Render 或 Railway | 均有免费额度 |

### 5.2 数据库设计（初版）

**表：articles（文章表）**

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | INTEGER (主键，自增) | 文章唯一标识 |
| title | TEXT | 文章标题，非空 |
| content | TEXT | 文章正文（Markdown 原文），非空 |
| created_at | DATETIME | 创建时间，默认当前时间 |
| updated_at | DATETIME | 最后更新时间 |

**表：users（P2 登录阶段新增）**

| 字段名 | 类型 | 说明 |
|---|---|---|
| id | INTEGER (主键) | 用户唯一标识 |
| username | TEXT | 登录用户名，唯一 |
| password_hash | TEXT | 加密后的密码 |

### 5.3 API 接口设计（初版）

| 方法 | 路径 | 说明 | 请求体 | 返回 |
|---|---|---|---|---|
| GET | /api/articles | 获取所有文章列表 | 无 | 文章数组（不含全文，仅摘要） |
| GET | /api/articles/:id | 获取单篇文章详情 | 无 | 单篇文章完整对象 |
| POST | /api/articles | 新建文章 | { title, content } | 新建的文章对象 |
| PUT | /api/articles/:id | 编辑文章 | { title, content } | 更新后的文章对象 |
| DELETE | /api/articles/:id | 删除文章 | 无 | { success: true } |

---

## 6. 里程碑与开发计划

| 阶段 | 内容 | 预计耗时 |
|---|---|---|
| 阶段一 | 后端基础框架 + 数据库设计 + CRUD API | 3-5 天 |
| 阶段二 | 前端页面骨架搭建（列表页/详情页/写文章页） | 3-5 天 |
| 阶段三 | 前后端联调 | 2-3 天 |
| 阶段四 | 样式美化 + Markdown 渲染 + 加载/错误状态 | 3-4 天 |
| 阶段五（可选） | 登录鉴权 + 标签/搜索功能 | 4-6 天 |
| 阶段六 | 部署上线 | 1-2 天 |

预计总耗时：**3-6 周**（视个人投入时间而定）

---

## 7. 验收标准

- [ ] 能在列表页看到所有已发布文章，按时间倒序排列
- [ ] 能点击进入文章详情，正确渲染 Markdown 内容
- [ ] 能成功新建一篇文章，并在列表页看到
- [ ] 能编辑已有文章，修改后内容正确更新
- [ ] 能删除文章，且有二次确认
- [ ] 前后端联调无 CORS 报错，接口返回数据正常
- [ ] 项目已部署上线，可通过公网 URL 访问
- [ ] 代码已托管在 GitHub，README 完整（含项目介绍、技术栈、本地运行方式）

---

## 8. 风险与应对

| 风险 | 应对方案 |
|---|---|
| CORS 跨域问题导致前后端联调受阻 | 后端使用 cors 中间件，明确允许前端域名 |
| 数据库设计后期需大改 | 初期字段设计保持简单，预留扩展空间（如 updated_at） |
| 开发时间拖延 | 严格按 MVP（P0）优先完成，P1/P2 功能可后置或砍掉 |
| 部署踩坑（环境变量、数据库连接） | 提前查阅 Render/Vercel 官方文档，参考现成案例 |

---

## 附录：后续可扩展方向
- 文章评论功能（引入简单留言板）
- 深色模式切换
- RSS 订阅支持
- 文章阅读量统计
